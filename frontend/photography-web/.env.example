# Leave blank for local development so Vite proxies /api to the backend.
VITE_API_URL=
# ASP.NET API target used by the Vite development proxy.
VITE_PROXY_TARGET=http://localhost:5284

# Optional Studio Profile map picker; manual coordinates work without this.
VITE_GOOGLE_MAPS_API_KEY=
